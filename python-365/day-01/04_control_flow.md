By default, Python code executes sequentially from top to bottom, one line at a time. 
Control flow structures allow you to break this default behavior to make decisions, repeat blocks of code, or handle exceptions

Python manages control flow using three primary mechanisms: 
-> Conditional Statements : if, else, elif, switch.
-> Loops (Iteration) : for , while.
-> Transfer/Jump Statements : break , continue, pass.
